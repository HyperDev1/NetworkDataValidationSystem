---
phase: 04-cicd
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - CICD-01
  - CICD-02
  - CICD-03

user_setup:
  - service: github-actions-secrets
    why: "GitHub Actions requires three repository secrets for Workload Identity and GCP auth"
    dashboard_config:
      - task: "Run scripts/setup-cicd.sh to get the secret values"
        location: "Terminal — export PROJECT_ID, GITHUB_REPO, GCP_PROJECT_NUMBER, then bash scripts/setup-cicd.sh"
      - task: "Add three repository secrets"
        location: "GitHub → repository → Settings → Secrets and variables → Actions → New repository secret"
        secrets:
          - WIF_PROVIDER
          - WIF_SERVICE_ACCOUNT
          - GCP_PROJECT_ID

must_haves:
  truths:
    - "A push to main triggers the GitHub Actions workflow within seconds"
    - "The workflow build-and-push job completes and a new image appears in Artifact Registry with sha-{commit} tag"
    - "The deploy job runs after build-and-push and Cloud Run is updated to the new image"
    - "Cloud Run service reflects the new image SHA after the workflow completes"
  artifacts:
    - path: ".github/workflows/deploy.yml"
      provides: "Workflow that triggered successfully"
  key_links:
    - from: "git push origin main"
      to: "GitHub Actions workflow run"
      via: "on: push: branches: [main] trigger"
      pattern: "Actions tab shows workflow run"
    - from: "workflow build-and-push job"
      to: "Artifact Registry image"
      via: "docker/build-push-action push: true"
      pattern: "new image tag sha-{sha} visible in Artifact Registry"
    - from: "workflow deploy job"
      to: "Cloud Run service"
      via: "google-github-actions/deploy-cloudrun"
      pattern: "gcloud run services describe shows new image"
---

<objective>
Run `scripts/setup-cicd.sh` to provision GCP infrastructure, add the three GitHub repository secrets, push a commit to `main`, and verify the full CI/CD pipeline executes end-to-end: build → push to Artifact Registry → Cloud Run deploy.

Purpose: End-to-end validation that Phase 4 is complete — the system now runs entirely in the cloud with automatic deployments.
Output: Working CI/CD pipeline; Phase 4 complete.
</objective>

<execution_context>
@/Users/beratcan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/beratcan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cicd/04-CONTEXT.md
@.planning/phases/04-cicd/04-01-SUMMARY.md
@.planning/phases/04-cicd/04-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Provision GCP infrastructure and set GitHub secrets</name>
  <files></files>
  <what-built>scripts/setup-cicd.sh (Plan 04-01) and .github/workflows/deploy.yml (Plan 04-02)</what-built>
  <action>
Human must run scripts/setup-cicd.sh to provision GCP infra (Artifact Registry, Workload Identity Pool/Provider, SA, IAM bindings), then add the three printed values as GitHub repository secrets (WIF_PROVIDER, WIF_SERVICE_ACCOUNT, GCP_PROJECT_ID). Claude cannot perform these steps: script runs gcloud commands requiring authenticated GCP session, and GitHub secret creation requires browser access to repository settings.
  </action>
  <how-to-verify>
1. Run the setup script to provision GCP infrastructure:
   ```bash
   export PROJECT_ID=your-gcp-project-id
   export GCP_PROJECT_NUMBER=your-project-number   # gcloud projects describe $PROJECT_ID --format="value(projectNumber)"
   export GITHUB_REPO=your-org/your-repo-name       # e.g. beratcan/NetworkDataValidationSystem
   bash scripts/setup-cicd.sh
   ```
   The script will print three values at the end under "=== Setup Complete ===".

2. Copy those three values and add them as GitHub repository secrets:
   - Go to: GitHub → repository → Settings → Secrets and variables → Actions
   - Add secret: `WIF_PROVIDER` = the printed WIF_PROVIDER value
   - Add secret: `WIF_SERVICE_ACCOUNT` = the printed WIF_SERVICE_ACCOUNT value
   - Add secret: `GCP_PROJECT_ID` = your PROJECT_ID

3. Confirm both steps are complete.
  </how-to-verify>
  <verify>Human confirms: setup-cicd.sh ran without errors AND all three GitHub secrets are visible in repository settings.</verify>
  <done>GCP infra exists (Artifact Registry repo + WIF pool + SA) and GitHub has WIF_PROVIDER, WIF_SERVICE_ACCOUNT, GCP_PROJECT_ID secrets.</done>
  <resume-signal>Type "done" when setup-cicd.sh has run successfully and all three GitHub secrets are added.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end pipeline on a main push</name>
  <files></files>
  <action>
Push an empty commit to main to trigger the workflow. Claude can run: `git commit --allow-empty -m "ci: trigger initial pipeline run" && git push origin main`. Then wait for the workflow to complete (Claude can monitor via `gh run watch` or instruct the human to check the Actions tab).
  </action>
  <how-to-verify>
1. Push any commit to main (or merge a PR):
   ```bash
   git commit --allow-empty -m "ci: trigger initial pipeline run"
   git push origin main
   ```

2. Watch the GitHub Actions workflow:
   - Go to: GitHub → repository → Actions tab
   - Find the workflow run triggered by your push
   - Confirm `build-and-push` job completes (green checkmark) — should take ~5-10 min
   - Confirm `deploy` job completes (green checkmark) after build-and-push

3. Verify image in Artifact Registry:
   ```bash
   gcloud artifacts docker images list us-central1-docker.pkg.dev/$PROJECT_ID/network-data-validation/app --include-tags
   ```
   Expected: A new entry with tag `sha-{your-commit-sha}` and `latest`

4. Verify Cloud Run is on the new image:
   ```bash
   gcloud run services describe network-data-validation --region=us-central1 --format="value(spec.template.spec.containers[0].image)"
   ```
   Expected: Image URI ending with `sha-{your-commit-sha}`

All four checks must pass for the phase to be complete.
  </how-to-verify>
  <verify>
    <automated>gh run list --limit 1 --json status,conclusion --jq '.[0] | "status=\(.status) conclusion=\(.conclusion)"'</automated>
  </verify>
  <done>GitHub Actions workflow completed successfully (conclusion: success); new image SHA visible in Artifact Registry; Cloud Run service updated to the new image SHA.</done>
  <resume-signal>Type "approved" if all four checks pass, or describe any failures.</resume-signal>
</task>

</tasks>

<verification>
Human-verified: GitHub Actions workflow ran end-to-end on main push with green status.
Human-verified: New image tag sha-{sha} visible in Artifact Registry.
Human-verified: Cloud Run service updated to the new image SHA.
</verification>

<success_criteria>
- CICD-01: push to main triggers GitHub Actions workflow build ✓
- CICD-02: built image pushed to Artifact Registry with sha-{sha} + latest tags ✓
- CICD-03: Cloud Run updated to new image automatically after push ✓
- Phase 4 CI/CD complete — milestone v1.0.1 fully automated
</success_criteria>

<output>
After completion, create `.planning/phases/04-cicd/04-03-SUMMARY.md`
</output>
